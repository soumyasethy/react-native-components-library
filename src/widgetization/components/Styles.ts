import { StyleSheet } from "react-native";
import { constants } from "../../constants";

export const styles = StyleSheet.create({
  titleWidget1: { fontSize: 20, marginTop: constants.spacing.xxlarge },
  titleWidget2: { fontSize: 16, marginTop: constants.spacing.xlarge },
  radioWidget: { fontSize: 16 }
});
