import { widgetMap } from './WidgetConstants'
import { registerWidget, createWidget, createWidgetWithProps, isRegisteredWidget } from './WidgetFactory'

export {
  widgetMap,
  registerWidget,
  createWidget,
  createWidgetWithProps,
  isRegisteredWidget,
}